---
import BaseLayout from "../layouts/BaseLayout.astro";
---

<BaseLayout title="页面未找到 | michaelwong.space">
  <div class="error-container">
    <div class="error-content">
      <div class="error-code">
        <div class="code-line">CDIV</div>
        <div class="code-line">肆零肆</div>
        <div class="code-line">404</div>
      </div>
      <h1 class="error-title">页面未找到</h1>
      <p class="error-description">
        Sorryyyyyyyyyyyyyyyy! 这个页面我还没写完……
      </p>
      <p class="error-hint">
        或者是不是你记错了？也有可能对吧……（对手指）
      </p>
      <div class="action-buttons">
        <a href="/" class="btn btn-primary">
          <span>返回首页</span>
        </a>

      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .error-container {
    position: relative;
    z-index: 1;
    padding: auto 5vw;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .error-content {
    text-align: center;
    max-width: 600px;
  }

  .error-code {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.7rem;
    margin: 0;
  }

  .code-line {
    font-family: "Bodoni Moda", serif;
    font-size: 4rem;
    font-weight: bold;
    background: linear-gradient(135deg, #6d0e11 0%, #a0111f 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
    line-height: 1;
    letter-spacing: 0.2em;
  }

  .code-line:nth-child(2) {
    font-family: "Noto Serif SC", serif;
  }

  .code-line:nth-child(1),
  .code-line:nth-child(3) {
    font-size: 2rem;
    background: none;
    -webkit-text-fill-color: unset;
    color: rgba(255, 255, 255, 0.5);
  }

  .error-title {
    font-family: "Noto Serif SC", serif;
    font-size: 2rem;
    color: var(--text-primary);
    margin: 1rem 0 1.5rem;
    letter-spacing: 0.05em;
  }

  .error-description {
    font-size: 1.1rem;
    color: var(--text-primary);
    margin: 1rem 0 0.5rem;
    line-height: 1.6;
  }

  .error-hint {
    font-size: 0.95rem;
    color: rgba(255, 255, 255, 0.6);
    margin: 0.5rem 0 2rem;
    line-height: 1.5;
  }

  .action-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
    margin-top: 2rem;
  }

  .btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.875rem 2rem;
    border: 1px solid rgba(255, 255, 255, 0.2);
    border-radius: 8px;
    text-decoration: none;
    transition: all 0.3s ease;
    font-size: 1rem;
    cursor: pointer;
    font-weight: 500;
    letter-spacing: 0.02em;
  }

  .btn-primary {
    background: rgba(109, 14, 17, 0.3);
    color: #fff;
    border-color: rgba(109, 14, 17, 0.5);
  }

  .btn-primary:hover {
    background: rgba(109, 14, 17, 0.5);
    border-color: rgba(109, 14, 17, 0.8);
    transform: translateY(-2px);
    box-shadow: 0 8px 20px rgba(109, 14, 17, 0.3);
  }

  @media (max-width: 768px) {
    .error-code {
      font-size: 5rem;
    }

    .error-title {
      font-size: 1.5rem;
    }

    .action-buttons {
      flex-direction: column;
    }

    .btn {
      width: 100%;
    }
  }
</style>
